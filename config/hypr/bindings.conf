# Application bindings
bindd = SUPER, RETURN, Terminal (Zellij agents), exec, uwsm-app -- kitty-agents
bindd = SUPER ALT, RETURN, Terminal (cwd), exec, uwsm-app -- kitty --session none -d "$(omarchy-cmd-terminal-cwd)" -e zellij
bindd = SUPER SHIFT, RETURN, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, B, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, omarchy-launch-browser --private
# bindd = SUPER SHIFT, M, Music, exec, omarchy-launch-or-focus spotify  # needs: flatpak install flathub com.spotify.Client
bindd = SUPER SHIFT ALT, M, Music TUI, exec, omarchy-launch-or-focus-tui cliamp
bindd = SUPER SHIFT, N, Editor, exec, omarchy-launch-editor
bindd = SUPER SHIFT, D, Docker, exec, omarchy-launch-tui lazydocker
bindd = SUPER SHIFT ALT, D, Discord, exec, uwsm-app -- discord-launch
bindd = SUPER SHIFT, G, Signal, exec, omarchy-launch-or-focus ^signal$ "uwsm-app -- signal-desktop"
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus ^obsidian$ "uwsm-app -- obsidian"
bindd = SUPER SHIFT CTRL, L, Save link to Obsidian, exec, /home/jackz/.local/bin/obsidian-save-link
bindd = SUPER SHIFT ALT, L, Open recent links, exec, /home/jackz/.local/bin/obsidian-open-links-recent
bindd = SUPER SHIFT CTRL, N, Quick note → Inbox, exec, /home/jackz/.local/bin/obsidian-save-note
bindd = SUPER SHIFT ALT, N, Multiline note → Inbox, exec, /home/jackz/.local/bin/obsidian-save-note --edit
bindd = SUPER SHIFT CTRL, M, Post note → j4c1cz.com, exec, /home/jackz/.local/bin/obsidian-post-note
bindd = SUPER SHIFT CTRL, COMMA, Note destination…, exec, /home/jackz/.local/bin/obsidian-note-dest
bindd = SUPER SHIFT, W, Wechat, exec, wechat-launch
bindd = SUPER SHIFT, SLASH, Passwords, exec, omarchy-launch-or-focus Bitwarden "uwsm-app -- bitwarden-desktop"
bindd = SUPER SHIFT ALT, C, Claude, exec, omarchy-launch-or-focus Claude "uwsm-app -- /home/jackz/.local/bin/claude-launch"

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bindd = SUPER SHIFT, A, ChatGPT, exec, omarchy-launch-webapp "https://chatgpt.com"
bindd = SUPER SHIFT ALT, A, Grok, exec, omarchy-launch-webapp "https://grok.com"
bindd = SUPER SHIFT, C, Calendar, exec, omarchy-launch-webapp "https://app.hey.com/calendar/weeks/"
bindd = SUPER SHIFT, E, Email, exec, omarchy-launch-webapp "https://app.hey.com"
bindd = SUPER SHIFT, Y, YouTube, exec, omarchy-launch-webapp "https://youtube.com/"
bindd = SUPER SHIFT ALT, G, WhatsApp, exec, omarchy-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"
bindd = SUPER SHIFT CTRL, G, Google Messages, exec, omarchy-launch-or-focus-webapp "Google Messages" "https://messages.google.com/web/conversations"
bindd = SUPER SHIFT, P, Google Photos, exec, omarchy-launch-or-focus-webapp "Google Photos" "https://photos.google.com/"
bindd = SUPER SHIFT, X, X, exec, omarchy-launch-webapp "https://x.com/"
bindd = SUPER SHIFT ALT, X, X Post, exec, omarchy-launch-webapp "https://x.com/compose/post"

# Add extra bindings
# bind = SUPER SHIFT, R, exec, alacritty -e ssh your-server

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu

# WeChat stays in the tray on close — killactive quits the whole app
unbind = SUPER, W
bindd = SUPER, W, Close window, exec, omarchy-close-window

# CleanShot-style captures: Super+Ctrl+1/2/3/4 (physical Caps = Ctrl after swapcaps)
bindd = SUPER CTRL, 1, Capture area, exec, omarchy-capture-screenshot region
bindd = SUPER CTRL, 2, Capture OCR, exec, omarchy-capture-text-extraction
bindd = SUPER CTRL, 3, Capture fullscreen, exec, omarchy-capture-screenshot fullscreen
bindd = SUPER CTRL, 4, Capture window, exec, omarchy-capture-screenshot windows

# Phone + personal agent
bindd = SUPER SHIFT, K, Phone KDE Connect, exec, uwsm-app -- sh -c 'kdeconnect-pair-help; uwsm-app -- kdeconnect-app'
bindd = SUPER SHIFT, T, Tasks (Obsidian), exec, omarchy-launch-or-focus ^obsidian$ "uwsm-app -- obsidian"
# Hermes gateway moved to Mac Mini — use Discord @Openclaw (not local TUI)
# bindd = SUPER SHIFT, J, Hermes agent, exec, uwsm-app -- xdg-terminal-exec --dir="$HOME/Obsidian/J4c1c" -- hermes --tui
bindd = SUPER SHIFT, J, Hermes Mac (SSH), exec, uwsm-app -- kitty --session none -e hermes-mac ssh
bindd = SUPER SHIFT ALT, J, Hermes logs, exec, uwsm-app -- kitty --session none -e hermes-mac logs
bindd = SUPER SHIFT, H, Hermes Discord webapp, exec, omarchy-launch-or-focus-webapp "Hermes Discord" "https://discord.com/channels/@me"

# Raycast-style audio output switch (Mac: Cmd+Alt+O) — also on Super+Mute by default
bindd = SUPER ALT, O, Switch audio output, exec, omarchy-audio-output-switch

# Hyprspace overview — use exec+hyprctl so config parse never needs the plugin loaded
# (raw "overview:toggle" as bind dispatcher fails at startup before hyprpm reload → safe mode)
bindd = SUPER, GRAVE, Workspace overview, exec, hyprctl dispatch overview:toggle
bindd = SUPER, code:41, Workspace overview, exec, hyprctl dispatch overview:toggle
bindd = SUPER SHIFT, GRAVE, Workspace overview (all monitors), exec, hyprctl dispatch overview:toggle all
bindd = SUPER SHIFT, V, Workspace overview, exec, hyprctl dispatch overview:toggle

# Logitech MX Keys
# bind = SUPER SHIFT, S, exec, omarchy-capture-screenshot      # Print Screen Button
# bind = SUPER, H, exec, voxtype record toggle                 # Dictation Button
# bind = SUPER, PERIOD, exec, omarchy-launch-walker -m symbols # Emoji Button

# LID_SWITCH: screen off on close, on when open (server mode)
bindl = , switch:on:Lid Switch, exec, hyprctl dispatch dpms off
bindl = , switch:off:Lid Switch, exec, hyprctl dispatch dpms on

# Waybar: docked = one bar on main external; laptop-only = sparse bar on eDP-1
# (profile switch is driven by waybar-monitor-watch.sh in autostart.conf —
# Hyprland binds can't intercept monitoradded/monitorremoved, only real input)

# Bedtime: blank all screens + lights, lid stays open (toggle).
# Super+Shift+Caps+S — with ctrl:swapcaps, physical Caps sends Ctrl, so bind is SUPER SHIFT CTRL, S
# bindld = also works while locked / input inhibited
bindld = SUPER SHIFT CTRL, S, Bedtime mode, exec, /home/jackz/.local/bin/bedtime-mode toggle

# Laptop LCD while docked — local override (see _meta/Hermes/studio-dock-lights.md)
unbind = SUPER CTRL, Delete
bindd = SUPER CTRL, Delete, Toggle laptop display, exec, /home/jackz/.local/bin/omarchy-hyprland-monitor-internal toggle


# Publish the clipboard URL to j4c1cz.com (see ~/.config/j4c1cz/config)
bindd = SUPER SHIFT ALT, L, Publish link to j4c1cz, exec, /home/jackz/.local/bin/j4c1cz clip
