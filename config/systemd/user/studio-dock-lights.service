[Unit]
Description=Studio Display dock lights (panel + keyboard backlight)
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/studio-dock-watch
Restart=on-failure
RestartSec=2

[Install]
WantedBy=graphical-session.target
