#!/usr/bin/env bash
# Mac setup helpers — does NOT uninstall user apps. Use macbook-obsidian-syncthing.sh for Obsidian sync.
set -euo pipefail

echo "This script no longer removes apps."
echo "Use: macbook-obsidian-syncthing.sh  (Obsidian Syncthing pairing)"
echo
echo "Safe to hide/remove manually (macOS stock, if you do not use them):"
echo "  - GarageBand, iMovie, Keynote, Numbers, Pages (if preinstalled)"
echo "  - News, Stocks, Home, Podcasts (remove from Dock / Login Items only)"
echo
echo "Do NOT remove: Safari, Mail, Finder, System Settings, or apps you installed via Homebrew."
